describe("globals", function () {
  it("should have a global pockets", function () {
    expect(pockets).to.be.ok;
  });

  it("should have a global pockets", function () {
    expect(pockets).to.be.ok;
  });

});